Instead of passing a dictionary, we should standardize the q -> parameters -> strain -> grad ...

Sooo, how are the parameters created?

Constants:
    stiff: EA, EI etc.
    r_len: voronoi_len etc.
    nat_strain: kappa_bar

Variables:
    nodes positions, matrerial directors


Soo, the only step wise variables we need are node positions and material frames: q, m1, m2, ref_twist


"""
if isinstance(K, np.ndarray):  # rod bending
            grad_strain = grad_strain.reshape(11, 2)
            gradE_strain = (del_strain.reshape(1, 2) @ K).flatten()
            hessE_strain = K
            grad_energy = grad_strain @ gradE_strain
            hess_energy = (gradE_strain[0] * hess_strain[0, :, :] + gradE_strain[1]
                           * hess_strain[1, :, :]) + grad_strain @ K @ grad_strain.T
            grad_energy = grad_energy.flatten()
        else:
"""